{
  "openapi": "3.0.1",
  "info": {
    "title": "Project X Voices & Music API",
    "description": "Manage battle states, auto-select music from Spotify, and generate ElevenLabs voices with synced emotions.",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "https://projectxmusic.onrender.com"
    }
  ],
  "paths": {
    "/setState": {
      "post": {
        "summary": "Set the current battle state (music + emotion)",
        "description": "Updates the battle state. Automatically changes Spotify playback to the correct track and adjusts emotion syncing for all voices.",
        "operationId": "setState",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "state": {
                    "type": "string",
                    "enum": [
                      "exploration",
                      "battle",
                      "boss1",
                      "boss2",
                      "boss3",
                      "victory",
                      "defeat"
                    ],
                    "example": "battle"
                  }
                },
                "required": ["state"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "State updated successfully",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "message": { "
